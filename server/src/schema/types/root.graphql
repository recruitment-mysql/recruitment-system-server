scalar Date
scalar JSON
scalar Cursor
scalar Upload
type Query
type Mutation
#type Subscription


type PageInfo {
    hasNextPage: Boolean!
    endCursor: Cursor
}
input PaginationInput {
    first: Int
    after: Cursor
    last: Int
    before: Cursor
}

enum SuccessResponse {
    success
}
